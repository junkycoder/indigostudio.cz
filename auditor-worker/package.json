{
  "name": "fakan-auditor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute fakan_auditor --file=./db/schema.sql",
    "db:init:remote": "wrangler d1 execute fakan_auditor --remote --file=./db/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14"
  }
}
